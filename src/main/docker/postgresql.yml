version: '2'
services:
  apiartisan-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/apiArtisan/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=apiArtisan
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
